#include <ctime>
#include "defs.h"

Search::Search(Evaluation evaluation)
{

};

Move Search::go(Board board, std::time_t timeAllowed)
{

};

Value Search::search(Board board, Value alpha, Value beta, Depth depth, int ply = 1)
{

};

Value Search::qsearch(Board board, Value alpha, Value beta, int ply = 1)
{

};
